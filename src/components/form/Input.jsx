export default function Input ({ placeholder }) {
    return (
        <input
          className="main_container_input"
          id="name"
          type="text"
          placeholder={placeholder}
        />
    );
}